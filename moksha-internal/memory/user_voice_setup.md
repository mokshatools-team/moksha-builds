---
name: Voice Input Setup
description: User's voice input configuration — SuperWhisper replaces macOS dictation
type: user
---

User uses SuperWhisper for all voice input. Replaced macOS built-in dictation.

- Hotkey: double `fn` (same muscle memory as macOS dictation)
- macOS Dictation: disabled in System Settings
- SuperWhisper output mode: Type (direct input, not clipboard)
- Works everywhere: VS Code, Claude Code terminal, Codex, any app
- Claude Code built-in voice (Hold Space) still enabled but SuperWhisper is primary
