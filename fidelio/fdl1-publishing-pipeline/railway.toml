[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir webhook-receiver app:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
